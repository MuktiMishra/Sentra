let count = 0;
while(true){
    console.log(count);
    count++;
}
//this put load on cpu lets see if metrics gets a hike or not
//lets run this in another terminal so running at 2 places means 2 differnt tasks
//lets do the same in 3rd terminal
//so we saw that we are fetching system metrics in every 5 secs and our metrics charts gets updated by hitting backend routes
// see now cpu load will fall down 